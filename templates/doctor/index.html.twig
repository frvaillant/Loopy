{% extends 'base.html.twig' %}

{% block title %}Loopy | Ajouter un nouveau patient{% endblock %}

{% block body %}

    <div class="container-fluid font-oswald ">
        <div class="row">
            <div class="col-9 pt-5 text-center bg-light-grey">
                <img src="{{ asset('/build/images/logo2.svg') }}" class="mb-5">

                <h1>Ajouter un nouveau patient au Programme Loopy</h1>
                {% for category, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ category }} text-center container mt-5">
                            {{ message }}
                            <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                        </div>
                    {% endfor %}
                {% endfor %}
                <div class="col-6 offset-3 mt-5 text-center bck-grey p-4">
                    {{ include('/doctor/patient/_new_form.html.twig') }}
                </div>
            </div>
            <div class="col-3 overflow" id="patient-list">
                {% include '/doctor/components/_patient_list.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript %}
{{ encore_entry_script_tags('ajaxValue') }}
{% endblock %}
